node_modules/
dist/
coverage/
*.vsix
.DS_Store
preview/
.claude/
# Scratch workspace for subagent-driven plan execution (ledger, briefs, diffs).
.superpowers/
# Design mockups are generated from the local ~/.claude tree and embed real
# plugin inventories — keep them out of this public repo.
docs/mockups/

# Playwright component-test artifacts.
playwright-report/
test-results/
playwright/.cache/
# E2E host downloads and reports.
.vscode-test/
playwright-e2e-report/

# Local scratch output dir suggested by docs/REACH.md for running the reach
# collector by hand — never meant to be committed to this repo.
.reach-data/

# scripts/sabotage.mjs's own marker, naming the patch currently applied so a
# killed run can be diagnosed and recovered. Root-level and gitignored (not
# under test-results/, which Playwright empties at the start of every run —
# that would erase the marker mid-loop).
/.sabotage-in-progress
