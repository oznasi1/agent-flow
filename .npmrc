# Pin the public npm registry so installs always resolve from npmjs.org,
# regardless of a contributor's global npm config. Keeps package-lock.json
# free of private-registry URLs.
registry=https://registry.npmjs.org/
